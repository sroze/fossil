package simplestore

import "testing"

func Test_Prepare(t *testing.T) {
	t.Run("can prepare two writes in parallel and successfully write them", func(t *testing.T) {
		// todo: prepare 1
		// todo: prepare 2

		// get segment position and lock
		// transform, write.

		// get segment position and lock
		// transform, write.
	})

	t.Run("two prepared statements transform on the same position fail", func(t *testing.T) {

	})
}
